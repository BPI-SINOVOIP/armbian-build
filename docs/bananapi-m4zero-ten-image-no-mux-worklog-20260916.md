# 十套映像免切換器執行紀錄

## 執行範圍

使用者核准開始執行[修訂計畫](bananapi-m4zero-ten-image-no-mux-plan-20260916.md)。本輪只操作既有 0845、其 UART 與明確配對電源，不操作其他串口或插座。初期未取得 eMMC 覆寫授權；完整備份後已另獲明確同意，範圍見下文。

主代理負責唯一實機操作及整合；獨立子代理分別負責矩陣盤點、主控台工具、唯讀備份與救援建置，修改範圍分開。每階段先保存結果再推送，不以執行中的測試作為通過證據。

## T1：原系統與傳輸基礎

證據目錄：`output/evidence/bpi-h618-no-mux/T1-0845-20260916/`。

1. 具名電源查詢核對開啟，串口無其他程序持有；固定入口回應 ABI3。未重刷 SPL1、FIT 或候選槽。
2. 經槽 3 載入既有 A1 配套鏈，正常進入原 SD 的 Armbian `25.2.2`，本次 `uname -r` 實收 `6.6.75-current-sunxi64`，root 登入成功。UART 開機紀錄 SHA-256：`8a269b4607db33b073f06a084a6c32342bda28226bc6c61465211c0c86408957`。
3. 先前新增的 SDIO Wi-Fi overlay 本次確實套用，`wlan0` 出現。使用主機已授權網路設定連線，不把密碼放進命令列、版本庫或 TX 紀錄。
4. 建立本次實驗專用 SSH 金鑰；經 UART 取得板子主機公鑰後固定至獨立 `known_hosts`，保持嚴格主機金鑰驗證。板上只追加受來源限制的專用公鑰，不放寬 SSH 密碼認證或改寫原 sshd 設定。私鑰與完整網路資料只在本機私有證據目錄保存。
5. 主機與板上 `/run` 完成各 64 MiB 的雙向傳輸，來源、板上與回傳摘要皆為 `68819252576a08579ba62aab8a716a37d8b3f9eb6c23451d5082e1cfc01e0997`。上傳約 12.15 秒、下載約 13.71 秒，含連線與程式啟動開銷，不視為網路吞吐極限。沒有向 eMMC 寫入測試檔。
6. 已啟動一小時 `stress-ng` 記憶體／CPU 驗證，使用 1 個 2 GiB VM 工作者、2 個 CPU 工作者、資料驗證及遇錯中止；執行中不記為通過。這是原系統引導基線的檢查，不是獨立救援 initramfs 的完成證據。

唯讀盤點報告 `linux-inventory-001.json` 的 SHA-256 為 `fef436d70d1005bf5a8137e8cd369501251c829ac040095db1c8b70afb068ccc`。雙向傳輸重新核對報告 `network-integrity-recheck-001.json` 的 SHA-256 為 `ba849a2559cfd6fa0374ceefe0537a0bae136f1da4b1266ac9decf459340f5c7`。

## 媒體編號與未完成事項

- SD 容量與 CID 仍符合既有紀錄，根系統位於 SD。
- 啟用 SDIO 後，eMMC 由先前的 `mmcblk1` 變為本次的 `mmcblk2`，容量為 `31289507840` 位元組，控制器為 `4022000.mmc`。先前以舊編號的唯讀查詢失敗保留於盤點，不把那些失敗寫成已完成 eMMC 檢查。新的查詢確認 `type=MMC`、CID 及控制器路徑。
- 最初未辨識到 eMMC 分割表或常見簽章，但未據此視為無資料。先完成下列完整唯讀備份，另取得授權後才部署。
- 開機仍有既有顯示 `debugfs` 警告，以及藍牙重設逾時、Wi-Fi 掃描部分頻道錯誤。Wi-Fi 成功連線不代表藍牙或所有頻道通過，不在救援傳輸工作中隱藏或擴大修理無關問題。
- 最新上傳目錄的 EMAC 候選為 `6.18.49` 五發行版各 minimal／XFCE 共十套。先做離線核對；舊 `6.18.48` 與 `6.6.75` 不刪除、不重編。普通 Zero 的結果不能替代未接擴充硬體的 EMAC 功能驗證。

## 工具與失敗紀錄

新增 `tools/bpi_lab_console.py`：借用既有排他串口介面，保留一次接收中尚未消耗的提示尾端，使用隨機、行邊界標記區分命令回顯與真正輸出。RX 日誌限制為新建私有檔，接收緩衝有上限；TX 不記錄。逾時不代表遠端命令已停止，呼叫端須另行確認。

主代理已執行 29 項主控台單元測試並通過；其中涵蓋提示尾端、ANSI 回顯、偽標記、逾時、短寫、日誌安全及上限。這是工具測試，不算 29 項硬體功能驗證。

兩次主機端腳本問題均保留原始證據，未因此重刷或重跑映像：

- `ssh-setup-001.bin`：多行 Python 命令遭主控台單行守門拒絕；改用明確單行編碼後在 `002` 完成公鑰追加，沒有放寬命令守門。
- `network-integrity-001.json`：雙向傳輸已完成，但主機 Python `3.10` 沒有 `hashlib.file_digest`，首次腳本因此沒有標記成功。改採相容串流雜湊，並對已存在的兩份 64 MiB 檔案執行 `sha256sum` 及長度核對，另存重新核對報告；原失敗報告不覆寫，沒有重傳相同資料。

## T0：十套固定清單

已固定 `google-drive-upload/2026/2026.08/bpi-m4z-emac` 的 `6.18.49` 十套，而非普通 Zero 或舊 `6.18.48`。主代理核對十筆壓縮摘要、XZ 完整串流、解壓摘要與長度、MBR 範圍及 eMMC 容量條件通過；續作第二輪十筆全部使用符合條件的既有證據，沒有重讀或重新產生映像。完整清單及來源提交見[盤點文件](bananapi-m4zero-image-inventory-20260916.md)。

這是離線檔案驗證，沒有任何候選的實板通過結果。開機組件與首次啟動行為另逐套檢查；沒有可信原始 `.img` 旁檔的項目，解壓摘要明列為本輪計算值。

## T2：完整唯讀 eMMC 備份

證據目錄：`output/evidence/bpi-h618-no-mux/T2-0845-emmc-backup-001/`。

- CID：`d629034339413535311299942ee08c07`；控制器：`/sys/devices/platform/soc/4022000.mmc`。實際裝置由身分查找，不把 `mmcblk2` 當永久編號。
- 全部 userarea：`31289507840` 位元組；原始 SHA-256：`e71138506555c3da6e8b4a180f4390e0f753dd572e460bedb2a407b2d889a4ed`。
- `emmc-userarea.img.gz`：`136536770` 位元組；SHA-256：`c0d61c7c5e5426da5dd7c5d8c7c5e9f183d529780062917420bd09a458bf29ec`。
- `manifest.json` SHA-256：`82e090733e6a8eac9f180f5fe37a1f142084d32274fe97678658552f87418e78`。工具與 SSH 均退出 `0`，狀態為 `complete`、`ok=true`，總耗時約 576 秒。
- 板端唯讀串流與主機落盤後解壓回讀的長度、SHA-256 一致；這不是第二次獨立讀取整顆 eMMC，也不是實際復原測試。
- 不含 boot0、boot1、RPMB；`restore_verified=false`、`write_authorized=false`。高壓縮率不是可刪除資料的授權。

唯讀備份工具先完成 23 項回歸，再於本次備份啟動前補至 30 項；主代理已重跑 30 項通過。原始備份及 SSH 私鑰不推送 Git；只提交可重用程式與摘要。備份期間原系統 CPU／記憶體壓測仍執行中，因此它也是並行負載下的讀取結果，不改稱獨立閒置測試。

備份執行後核對的主機來源 SHA-256 為 `b342e99ac85d8b83b8565da31c2fbc32805f60e9c27471b82a81a570009cf84a`；manifest 內嵌遠端程式 SHA-256 為 `7b9acafaddfda4d558dc54ef0d3cf2f7cd76725d015e23535243707461340c23`。前者未於啟動時寫入 manifest，不能追補成當時已有的來源證明；後者與目前程式逐字相符。後續工具修正不覆寫這份原始證據。

## T1：獨立救援建置

在原 0845 Linux 以現有 `6.6.75-current-sunxi64` 核心及模組，於 `/root/bpi-lab/rescue-build-20260916-001` 真正執行 `mkinitramfs`。使用私有 mount namespace 隔離原共享 hooks；只寫新的工作目錄，不改原 `/boot`、SPL1、FIT、槽或 eMMC。

成品 `rescue-initramfs.img` 為 `28133455` 位元組，SHA-256 為 `1bb869de199636881240a4fa25e5d1dfc8fb6e10e246d4d55768a49c48058d24`；回傳主機摘要一致，`gzip -t` 通過。建置報告、清單及日誌回存在 T1 證據目錄，報告 SHA-256 為 `a0874c24ecc2d0f59a001e99c62fb93bf514acb9b6cf5e8a9086f7d675762e55`。

救援工具 17 項離線測試通過。建置曾顯示 brcmfmac 其他晶片韌體缺失警告，原樣保留；成品清單已含這片使用的 BCM43455 韌體及法規資料。是否能冷開機、載入 Wi-Fi 及 SSH，仍以後續實板結果判定，不能僅憑清單宣稱成功。

新增的一次性 U-Boot 載入器先核對既有橋接封包，僅從槽 3 交接；核心、DTB、overlay、救援包載入後比較長度及 RAM CRC32。現有 U-Boot 沒啟用 `hash` 命令，CRC32 僅為傳輸錯誤檢查；可信 SHA-256 由原 Linux 經固定主機金鑰的 SSH 取得，不把 CRC32 說成密碼學驗證。不執行原 `boot.scr` 或保存環境；只套用已核對的 Wi-Fi overlay，不套用未指定的 pin 參數。此路徑的差異屬救援環境，不作客戶原生啟動結果。

## 下一步

使用者已於本輪明確同意：「備份核對後可覆寫 eMMC」。此授權只涵蓋上述 0845、CID 尾碼 `e08c07` 的 userarea；不涵蓋 SD 救援區、boot0、boot1 或 RPMB。原始備份 manifest 的 `write_authorized=false` 是備份當時狀態，保持不可變，授權於此另記，不回頭修改原始證據。

原系統基線壓測已完成，SSH 退出 `0`，`stress-ng` 回報 `3600.46` 秒成功完成。完整壓測日誌 SHA-256 為 `440bc99793b5e093bff05a110eb86b665df8beb641fef161a78c0821fe8a94db`；`baseline-final-001.json` 保存原始輸出、核心日誌、溫度、掛載與服務狀態。沒有失敗服務或殘留 stress-ng。UART 被動擷取期間為零位元組，只表示該時段未收到新輸出，不把空日誌當成硬體通過證據。

依使用者最新要求，先完成一套端到端實測，再擴到十套；十次冷啟動與額外一小時獨立救援壓測移到後續可靠性測試，不再作第一套部署前提。未執行的測項不記為通過。

## T1：救援實機修正與通訊

- `T1-rescue-cold-001` 已實際進入 RAM 救援，但核心訊息插入 UART JSON，工具拒絕破碎資料。改為最多三次唯讀重讀，不拼接原始證據。
- `T1-rescue-network-001` 發現缺少 `brcmfmac_wcc`，無 `wlan0`。主機自行正常關機、斷電後回原 SD；登入器補認得中文密碼提示，未拔卡、未改帳密。
- 救援 `002` 加入該模組，產物 SHA-256 為 `6dc672b0fa4ccefb4e7c9c646e3fe1504c776a3532f81996c045e5a0adcd567b`。`T1-rescue-cold-002` 已驗證 RAM 根、無 MMC 掛載、無 swap，Wi-Fi DHCP 成功。
- 後續位址查詢誤選 BusyBox `ip`，改用已存在的 `/usr/bin/ip`，在同一救援實例接續完成，不重建、不重啟。`T1-rescue-network-002` 經 UART 固定新主機公鑰，嚴格 SSH 成功；盤點 SHA-256 為 `ef7359e64f66175b3b2219358629897c82e3f5274bc9eb5e31e5bd8b721c61a6`。

## T3：第一套實際部署

`T3-bookworm-deploy-001/receipt.json` 已正式發布，工具退出 `0`、狀態 `verified`。來源為既定十套內的 Bookworm minimal，不是重編或重新打包版本。

- 寫入 eMMC userarea 的範圍為 `[0,1543503872)`，同步及清除裝置快取後完整回讀通過；SHA-256 為 `0677fe8aa9624b07232e8257c3fe1f4686178402083fdb18d20ad410efb7c8e0`。
- SD 前 4 MiB 寫入前後皆為 `671150514358527a9a3f76c4a949a9d80db8ef29e40187bf1812d6344b39e88d`。未寫 SD、boot0、boot1、RPMB。
- 救援無 RTC 校時，遠端部署紀錄顯示 1970 年，不能用它判定實際日期；主機收據時間才是本輪排程時間依據。
- 此收據只證明部署與回讀，不代表開機或完整系統測試通過；引導結果另記。

## T3：第一套端到端實測結果

Bookworm minimal 已完成「救援部署 → 客戶核心與根系統 → 基本測試 → 正常關機 → SD 救援」完整循環，全程未拔卡。這是系統層的流程驗證，不是整套功能或長期穩定性通過。

| 測項 | 結果 |
| --- | --- |
| 原配套核心 | 實收 `6.18.49-current-sunxi64`，不是救援 `6.6.75` |
| 根系統 | `/dev/mmcblk1p1`，UUID `62cd58de-498d-4325-8528-96a15da7d902`；父媒體 CID 與指定 eMMC 一致 |
| SD 保護 | 候選 RAM DTB 停用 SD host；Linux 只看見 eMMC；回救援後 SD 前 4 MiB 不變 |
| CPU | 兩個工作者各約 28.8 秒 CPU 時間，固定 SHA-256 重算全部符合 |
| 根檔案系統 | 自有 16 MiB 檔案寫入、fsync、單檔快取丟棄提示、回讀摘要與清理通過；不冒充實體全碟測試 |
| 有線網路 | UART 固定 SSH 公鑰後連線成功，64 MiB 上傳與回傳摘要一致 |
| Wi-Fi | RAM 設定連至 NA3，指定 `wlan0` 對主機 5 次 ping 全部回應；未測長時間吞吐 |
| 系統服務 | `console-setup.service` 失敗，`setupcon` 回報暫存鍵盤檔不存在；整體不標全數通過 |
| 記憶體 | `/run` 的 `noexec` 首先拒絕測試工具；改放一次性 root 工具後，256 MiB 完整一輪未於 300 秒內結束，記為逾時未完成，不判 DDR 錯誤 |
| 回救援 | 正常關機、斷電至少 10 秒、固定 SRAM 入口、RAM 救援、Wi-Fi 與嚴格 SSH 均成功 |

UART 初次 DTB 查詢因 U-Boot 不輸出分號而被工具拒絕，修正可選分號後直接續接既有載入狀態，未重新燒錄或重啟。首次 root 初始化設定隨機密碼並依原流程略過普通使用者；這是測試設定差異，沒有宣稱桌面登入通過。

有線介面實收 MAC `de:ad:be:ef:00:01` 與 100 Mbps 連線，需另查預設 MAC 來源及多板衝突風險；本次單板連線成功不能證明可直接多板量產部署，也不推定擴充板硬體版本。

關鍵證據：

- 部署：`T3-bookworm-deploy-001/receipt.json`，SHA-256 `ac089cc2d00cd318f88c8fbfa5a9712085dca28e2d8cebd37fd44bddf8ef5765`。
- 登入與盤點：`T3-bookworm-firstlogin-002/report.json`，SHA-256 `2243366da8302a411382da420ff0d16be5cfec3165d6f4d7f28c3599c13aa9fe`。
- CPU／檔案系統：`T3-bookworm-smoke-001/report.json.partial` 為完整收集但有失敗測項的報告；不是成功收據。
- 未完成記憶體原始日誌：`T3-bookworm-memory-002/memtester.partial.log`，SHA-256 `9130adcb56179a894c9f91c8f9f6d84aeb43defc7a0314022493478311be8200`。
- 回救援：`T3-bookworm-return-001/report.json`，SHA-256 `dee441b8c0b900678820ca7b83aaca839482dab907a1a299cc2f094410221c51`。

## T4：批次與第二套部署中斷

首套實測及工具已推送 `dcc312b18`。剩餘九套批次已啟動，先測 Bookworm XFCE；沒有重刷已完成的 Bookworm minimal。桌面版的 `xfce_desktop` 識別與 `bootlogo=true` 已依真實清單及原 `boot.cmd` 支援，保留 `splash plymouth.ignore-serial-consoles`，不更改來源 manifest。

`T4-matrix-001/bookworm-xfce_desktop/deploy/` 在寫入 `2012710558` 位元組時收到板端 LZMA 錯誤，目標全長為 `5091885056`。沒有正式成功收據，不啟動半寫入候選；SD 前綴仍一致，救援及 SSH 保持可用。當時主機已完整解壓核對同一原檔，不能直接歸咎原檔、DDR、網路或 eMMC；先在 RAM 單獨核對傳輸及解碼。原始失敗及已完成的首套證據均保留。

此批次的整體及目前案例狀態均為 `interrupted`，不是仍在背景燒錄。後續重試必須使用新的嘗試目錄，不能把半寫入進度當成可開機或略過的項目。

`T4-xfce-xz-probe-002` 改以普通 SSH stdin 傳至新建 RAM 檔案，再用板端標準 `lzma.open` 解碼，不寫 MMC。壓縮檔 `1016372620` 位元組，前後 SHA-256 均為 `05a1505dc3a7eb044afb12f84dbdc93fe1c4e576cee077ec0aeda7acd0a52748`；解壓 `5091885056` 位元組，SHA-256 為 `976d0c251f0effa2ef286f27809b39c6aee7ec9c16a6a72e986499b91e932e08`，全部符合，解碼約 304 秒。已刪除僅本次產生且核對相同的 RAM 副本，原 XZ 不動。

此結果證明該次傳輸及獨立解碼正確，尚未定位原串流部署失敗原因。已補保留 LZMA 原始例外及失敗前壓縮資料「已讀」長度／SHA-256、已送出解碼資料摘要；「已讀」不等於解碼器已消耗全部輸入。另開 `T4-matrix-002`，第二套只重試一次，不回填或刪除原失敗；若再失敗即用摘要定位，不繼續盲目重試。

## T4：第二套完成及原地續作

Bookworm XFCE 的一次限定重試已完成：寫入全部 `5091885056` 位元組、完整回讀符合原始解壓摘要，部署共約 427 秒。這次成功不等於前次偶發 LZMA 錯誤已定位或修復。

候選已進入原配套 `6.18.49-current-sunxi64`，首次 root 初始化完成。自動連線先遇到 `/etc/ssh/ssh_host_ed25519_key.pub` 尚未產生；後續 UART 唯讀查詢確認原 `armbian-firstrun` 正在重新產生主機金鑰並重啟 SSH，服務最終正常。沒有重設 SSH、放寬主機身分驗證或重刷映像。

新增有期限的 SSH 就緒等待後，`T4-matrix-003` 核對既有部署與開機收據，直接從連線階段續作，沒有再次部署或重開這套 XFCE。實收根 UUID `5b0abf3b-e889-4cbb-8d70-96abd76024bd`、eMMC CID、CPU 雙工作者摘要與 16 MiB 檔案讀寫均符合；`console-setup.service` 仍失敗，保留原狀。

第二套已正常關機並回到 RAM 救援，SD 前 4 MiB 摘要不變。已完成循環為 2／10，整套硬體測試通過仍為 0；普通使用者、桌面登入、GPU、周邊與記憶體長測不在此次短測內。批次已續至 Jammy minimal，不重做前兩套。

| 證據 | SHA-256 |
| --- | --- |
| `T4-matrix-002/bookworm-xfce_desktop/deploy/receipt.json` | `8fc6eb51d8fcb1421865cf27f56cd0f694b00b0bfc64f0484fd6fd29bb9be101` |
| `T4-matrix-002/bookworm-xfce_desktop/boot/report.json` | `4765f3c49659bc7250b1991ab892a98c9790359d5125fa1658b7f82690ef9bdb` |
| `T4-matrix-003/bookworm-xfce_desktop/smoke/report.json.partial` | `8afb45a48ac6af3cc9cefbe1c6510fdd4fa734ed641878c8e73efc5749843ce4` |
| `T4-matrix-003/bookworm-xfce_desktop/return/report.json` | `13d8c07b250d261cbc265281ff51eb8213622cf8acf5ddac2a3f9c13dc8e7106` |

## 擴展數量與效率界線

本機 `google-drive-upload/2026/2026.08` 的目錄盤點為 45 個板別、444 個 `.img.xz`；`bpi-cm6`、`bpi-f3`、`bpi-sm10` 各 8，其餘各 10。這是檔案數量，不是 444 套已完成硬體驗證，也不是重新讀取 444 份巨檔後的完整稽核。

- 首次媒體備份每片做一次；每套仍保留身分守門、寫入摘要及完整回讀，不用省略安全檢查換速度。
- 同一硬體的 OS 批次共用已驗證救援；已部署、已開機者從實際中斷階段續作，不重刷。
- 主機以固定組件及映像雜湊保存離線核對結果；只重做來源或測試條件真正變更的項目。
- 每套先完成基本系統測試；同一組核心／DTB／韌體的深入硬體測試另外分組，但不把代表組結果冒稱各套已逐一長測。
- 同一 UART、電源與媒體只由一個操作流程控制；未來多板使用獨立配對的測試站並行，不在單片上並行改寫媒體。
- 其他板型共用清單、傳輸、證據與續作格式；救援入口、電源配對、CID、DTB 及儲存控制器必須個別適配。H618 的位址與 MMC 編號不得直接套到其他 SoC。

## T4：第三套與跨 OS 介面差異（2026-09-17）

Jammy minimal 部署及完整回讀約 143 秒，實際載入原 `6.18.49` 核心並完成 root 初始化。連線工具原先只選 `end0`／`wlan0`，但此系統使用 `eth0` 且已取得 DHCP；這是工具假設錯誤，不是網路功能失效。

`tools/bpi_lab_network.py` 改以到主機的實際 IPv4 路由、來源位址及介面狀態交叉核對，不再假定介面命名；四項回歸涵蓋多種名稱、錯路由、未就緒及不一致位址。SSH 等待也改為先確認原 `armbian-firstrun` 執行完畢，再固定 SSH 主機金鑰，避免抓到首次啟動過程中即將更換的金鑰。

`T4-matrix-004` 核對原部署及開機收據後，從 Jammy 的連線階段續作，略過已完成的兩套 Bookworm。Jammy minimal 的核心、根 UUID／CID、嚴格 SSH 與 CPU 短測通過，沒有失敗服務；檔案測試因可用空間不足以容納 16 MiB 測試檔及保留 32 MiB，而在寫入前拒絕。這輪停用了自動擴容，因此此項屬受控測試條件不足，不能直接推論客戶正常擴容後也無法使用。

第三套已正常回到 RAM 救援且 SD 前綴一致，完成循環為 3／10；第四套 Jammy XFCE 接續部署。原始 `003` 連線失敗報告不覆寫，`004` 獨立保存續作結果；沒有為連線工具修正而重刷 Jammy。

## T4：第四套完成（2026-09-17）

`T4-matrix-004/jammy-xfce_desktop` 已完成 `4638900224` 位元組部署、完整回讀、原配套核心開機、首次 root 初始化、嚴格 SSH、CPU、16 MiB 檔案讀寫及回救援。短測未見失敗服務，為目前第一套 `smoke_all_passed=true`；不擴大為桌面登入、GPU、周邊或長測通過。SD 前綴仍一致。

截至此筆記錄，完成基本循環 4／10，第五套 Noble minimal 依序部署中。新增的唯讀摘要工具已以實際四輪批次資料核對，能保留先前 XZ、SSH 首次啟動與介面命名造成的中斷；不用重做已完成的映像來重建進度。

## T4：第五套完成（2026-09-17）

`T4-matrix-004/noble-minimal` 已完成部署、完整回讀、原核心／根系統身分核對、首次初始化、嚴格 SSH、CPU 短測與回救援，未見失敗服務，SD 前綴一致。16 MiB 檔案測試同樣因未擴容的可用空間不足而未執行，保留 `blocked` 條件限制，不判定映像檔案系統故障。

完成基本循環 5／10，第六套 Noble XFCE 部署中；基本短測全通過目前仍為 Jammy XFCE 一套，全部硬體功能與長測通過數仍為零。

## T4：第六套 RCU 停滯及救援（2026-09-17）

Noble XFCE 已完成全長 `4995416064` 位元組部署及完整回讀，但第一次客戶核心開機出現多次 `rcu_preempt` stall／計時與排程警告，未到登入畫面，工具有界逾時退出。這不是介面名稱或 SSH 等待問題，也不記為開機成功。

- 失敗報告：`T4-matrix-004/noble-xfce_desktop/boot/report.json`，SHA-256 `dda4c056295d409b921e0d0e225a7cf241bd4e623e3437bd42ded49506ad6209`。
- UART：同目錄 `uart.bin`，SHA-256 `d7e383f649e9f3c18a34ec85ba238eb9b8e94f2fbff546875824c226d40cb157`。
- 核對已結束的失敗報告後，主代理控制已配對電源斷電至少 10 秒，回到固定 SRAM 入口及 RAM 救援，Wi-Fi、嚴格 SSH、SD 前綴核對全部成功。此為強制斷電，未冒稱正常關機；記錄在 `return-after-hang-001`。
- `readback-after-hang-001` 在 RAM 救援唯讀重新核對整個映像範圍，約 43 秒完成；SHA-256 仍為 `fca0806eb4b5e0914e457689675abb32285f2519f850b5a543097b2e350b555e`，與原映像相同，沒有重刷。這只排除本次讀取可觀察的媒體內容改變，不能單獨定位 DDR、核心或韌體原因。

`T4-matrix-005` 略過前五套，只重試一次 Noble XFCE 冷啟動，沿用已重新核對的部署內容；不是重新部署。後續若開機失敗，先保留完整失敗報告，成功回救援後記為開機失敗，再測下一套，不將失敗無限重試至偶發成功。救援未成功或沒有完整失敗證據時停止媒體操作。
