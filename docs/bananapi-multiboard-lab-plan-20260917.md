# Banana Pi 跨板映像自動測試計畫

日期：2026-09-17。分支：`bpi-h618-recovery-network-20260915`。

## 目標與本輪界線

使用者要求先完成計畫並推送，再直接完成可離線實作的部分，之後才提供各板實測。目標是把目前 45 板別、444 套映像納入同一套可續作流程，避免重複燒錄、反覆人工追進度或為每片板重造工具。本輪不操作電源、UART 或儲存媒體，不重做 0845 已完成的十套基本循環。

軟體驗收與硬體驗收分開。離線完成不代表各板可開機，更不代表 444 套已通過；模擬結果不得匯入硬體通過數。先前 0845 的 RCU／LZMA 間歇失敗、服務失敗、缺測及共用 A1 引導差異保持原紀錄，不因新工具而洗掉。

## 設計與效率

1. 唯讀盤點既有映像及 SHA-256 旁檔，解析板別、OS、角色、架構與 SoC 家族。初次盤點不解壓 444 個巨檔；旁檔是宣告值，不當成實際內容核對。解析 shell 設定只能讀文字，不能 `source` 執行。
2. 排序固定為架構、OS、板別、映像角色。板型資格測試可另依 SoC 家族分組；同站一次一套，多個已配對站點可獨立並行，不讓同一 UART、電源或媒體被同時操作。
3. 每項工作綁定映像雜湊、站點與實體板號、引導配置摘要、測試版本。改任一條件就建立不同工作；不靠檔名或既有檔案存在判定通過。
4. SQLite 保存工作、逐階段結果、嘗試及事件；事務與站點鎖防重複領取。結果檔先持久保存再更新狀態。逾時或程序中斷保留未知／待恢復，不能自動重刷半寫媒體或假裝前一步完成。
5. 執行階段為前置核對、部署與回讀、客戶核心啟動、短測、返回救援。每階段有期限及獨立報告；短測失敗仍須回救援。故障返回失敗立即隔離站點，不自動處理下一套。
6. 沿用既有工具的媒體身分、完整回讀、原配套核心、UART 與嚴格 SSH 原則。不得為省時間跳過必要回讀；來源靜態核對才可依完整檔案身分及已核定雜湊快取。
7. 平台適配器與通用排程分離。適配器使用明確參數、固定程式摘要及有界子程序，不以 shell 拼接映像內容。報告綁定本次工作與嘗試，另記是否模擬，不接受模擬報告冒充實板。

## 站點與安全條件

每個站點必須明示實體板型、板號、可用映像板別、UART 穩定路徑、電源配對、測試媒體 CID／容量／控制器、救援配置摘要、可覆寫範圍及授權來源。沒有提供硬體時只建立停用範本；不從映像名稱推測 HW_ID、不把 M4 Zero EMAC 映像當成實體擴充板已驗證。

寫入僅由已核定平台適配器執行。備份核對及覆寫授權須綁定該片媒體，不能把 0845 授權套到其他板；SD 救援、boot0／boot1、RPMB、OTP／eFuse 不在一般映像批次授權內。無 eMMC 的板子需另核定 USB／網路測試區，不能假造 eMMC 路徑。

H618 的 SRAM 位址、FIT 偏移及 MMC 編號不能套到其他 SoC。現有 0845 腳本作為已實測原型參考；正式站點適配器須透過新契約及實板核定，不能把硬編原型改板名後就當通用支援。

## 階段與交付

| 階段 | 本輪交付與驗收 | 初始狀態 |
| --- | --- | --- |
| P0 | 計畫先提交及推送，之後才改工具 | 已完成，`4aa4a87c7` |
| P1 | 全目錄唯讀盤點、45 板候選清單、架構／OS 排序；異常與未知欄位明列 | 已完成：45 板／444 套；F2P／F2S 共 20 套核心版號未知，保持問題標記 |
| P2 | 嚴格站點設定與平台執行契約、停用範本、禁止模擬升格實測 | 已完成契約及拒絕測試，實體資格仍待 H1 |
| P3 | 持久排程、同站排他、事件與結果保存、階段續作、有界失敗返回 | 已完成；收據恢復、救援分支持久化、跨資料庫資源隔離及故障回歸通過 |
| P4 | CLI 整合，實際 444 筆工作離線匯入及去重；假資料模擬成功、中斷、逾時、報告錯配、並行衝突，不觸碰硬體 | 已完成離線驗收：424 筆模擬完成、20 筆資料阻擋；再次匯入／執行均零重做 |
| P5 | 本機回歸與獨立審查、操作手冊、完成／待硬體項目表；提交與推送 | 本機 524 項回歸與限定複審通過；操作手冊及完成紀錄隨本階段提交推送 |
| H1 | 使用者提供實板後，配對站點、備份、核定寫入及平台引導，先一套完整循環 | 待硬體，不算本輪離線完成 |
| H2 | 同站 OS 批次短測，按實際組件分組深入測試；各板功能、冷啟動與長測分開記錄 | 待 H1 |

本輪完成條件：P0 至 P5 的軟體功能、444 筆離線工作與續作示範有可查證輸出；所有未具備實板資格的站點維持禁止寫入。平台實板適配與測試必須留在 H1／H2，不以模擬或設定檔數量宣稱完成。

## 接板後的最小流程

1. 提供板型／修訂、板號、DDR、供電、UART 接線及可覆寫媒體；一次確認電源與媒體配對。
2. 核定獨立救援路徑及大檔傳輸；先備份並核對、取得該媒體授權，再做一套完整循環及故障回救援。
3. 證據通過後啟用站點，依佇列測同板各 OS。保持來源原檔、失敗資料及唯一嘗試，不重做已符合相同條件的階段。
4. 先同系 H618，再按已取得硬體與適配成熟度擴至其他家族；不為等待某片板而阻塞其他獨立站點。

## 進度紀錄

每個完成階段更新本文件及機器可讀狀態並推送。原始映像、帳密、私鑰及完整媒體備份不提交 Git；只提交工具、設定範本、去識別化測試資料及摘要。既有十套證據仍以[第一輪總結](bananapi-m4zero-ten-image-first-pass-20260917.md)為準。

## 本輪交付結果

計畫先推送 `4aa4a87c7`，盤點與站點契約階段推送 `2b8efe0ec`；共用軟體、演練及操作文件已完成，見[操作手冊](bananapi-multiboard-lab-guide-20260917.md)與[機器可讀完成紀錄](evidence/bpi-multiboard-lab-20260917/completion.json)。本輪沒有操作實體 UART、電源或媒體，沒有重新產生或刷寫客戶映像。

524 項為 168 項主控台／網路／跨板工具測試加上 356 項既有 H618 回歸。獨立審查找到的失敗游標、收據落盤與入庫間隙、跨站隔離、工作鍵及模式污染、救援再次中斷、模擬領取競態，均已修正並納入回歸。限定複審無剩餘阻擋，不等同完整形式驗證或實板認證。

444 筆來源均有工作紀錄；20 筆核心版本未知保持阻擋，不降低標準來湊通過數。414 筆硬體工作等待配對，另十筆因已具 0845 歷史證據先待審；45 個實體站點範本全部停用。真正平台後端仍有未完成工作，不能宣稱只有插線就能測遍所有 SoC。

## 後端實作續階段：不限 H618

使用者再次要求其他平台同步推進。更正原本把全部平台適配工作放在 H1 的界線：程式、來源核對及離線回歸現在就做；媒體配對、首次救援部署、引導及故障恢復的實體資格才等待各板。H618 是已有實測原型的先行平台，不是唯一交付對象。

| 階段 | 交付 | 本階段結果 |
| --- | --- | --- |
| A0 | 本補充計畫先提交並推送 | 已完成，`8ed9d3f51` |
| A1 | 從目前建置設定及引導腳本建立完整 45 板家族表、來源摘要與能力檢驗器；逐板標示實作、限制及未知，不以架構相同推論開機鏈相容 | 已完成來源候選及守門：45 板、14 群組、17 種入口、98 份來源；40 項測試通過 |
| A2 | ARM32、ARM64、RISC-V 的一次性 U-Boot 載入與引導實作；明確配置 RAM、保留區、格式、載入來源及原配組件，逐次核對長度及摘要，禁止持久環境或媒體寫入 | 已完成限定格式的 runtime 與離線 CLI，35 項測試通過；不包含任意 FIT、原廠容器、extlinux 或板級救援 |
| A3 | 跨平台 Linux 真實唯讀預檢與結果核對；由根掛載追到實際媒體，不硬編 H618 的 CID、控制器或核心版號；不支援的根儲存堆疊明確阻擋 | 已完成真 SSH 收集與離線核對，56 項測試通過；直接 MMC／SD 根媒體以外保持阻擋，未執行負載測試 |
| A4 | 把既有 H618 原型接入正式階段契約，優先前置核對、已授權部署與短測；保留現有 UART 引導及救援實作的板號限制，不把部分接線冒稱完整平台適配 | 已接入三階段，16 項接線測試及 43 項部署測試通過；十套真實本機組件綁定核對通過；boot／recovery／resume 尚未接入 |
| A5 | 整合各平台清單、工具回歸與拒絕測試；逐項列出哪些已有可執行程式、哪些仍缺專用引導／救援實作，提交及推送 | 整合 677 項測試及 Ruff 通過；限定部署安全複審無剩餘高訊號問題，交付隨本階段提交 |

按來源分類處理 Allwinner ARM32／A64／H618、Amlogic、Rockchip ARM32／ARM64、MediaTek、SpacemiT K1／K3、Sunplus、Renesas、Realtek 與 Synaptics。共用 U-Boot 命令只能覆蓋格式及能力相容的部分；簽章、FIT、原廠容器、SBI／TF-A、媒體啟動優先序與 ROM 救援仍須分家族核定。禁止把 H618 的 SRAM 載入位址、固定 SD 入口或 eMMC 授權複製到其他平台。

本階段不操作硬體、不重刷已測十套、不重新編譯 444 個客戶映像，也不啟用任何未核定站點。軟體驗證使用小型測試資料及實際本機建置來源；合成的 UART／SSH 回應只證明程式行為，不計入硬體通過數。後續實板接入仍使用 H1／H2，但不再以缺板子作為停止 A1 至 A5 的理由。

### 交付入口與後續缺口

全板清單與逐家族差異見[跨平台來源表](bananapi-lab-platforms-20260917.md)，操作方式分別見[U-Boot 引導](bananapi-lab-uboot-20260917.md)、[Linux 預檢](bananapi-lab-linux-20260917.md)及[0845 階段接線](bananapi-lab-h618-adapter-20260917.md)。[機器可讀交付紀錄](evidence/bpi-multiboard-backends-20260917/completion.json)不混入硬體通過數。

實作已依序提交及推送：Linux 預檢 `c6354bb72`、跨架構 U-Boot `acae67e25`、0845 接線與部署補強 `cf0a15180`；本次平台清單及總結隨後單獨提交。既有 `main` 未變更。

677 項包含跨板工具 315 項及 H618 工具 362 項；上述單項測試數是其中子集，不能再相加。部署獨立審查找到預檢至寫入間的固定條件變動缺口，已補入寫入前核對、SSH 固定副本及來源驗證後的備份身分重驗；錯配回歸要求零傳輸或零目標寫入。限定複審不代表完整形式驗證。

仍有軟體工作，不能把 45 板標為「只差硬體」：

1. H618：完成電源、SRAM／UART、首次登入、SSH 換接及故障返回的正式契約，保留原型既有失敗證據。
2. 同步推進 Allwinner ARM32／A64 共 13 板及 Amlogic 4 板：建立原配組件擷取、板級 RAM／媒體配置、overlay／fixup 處理和獨立救援。不等待 H618 全部收尾才開始。
3. Rockchip 9 板、MediaTek 7 板、SpacemiT 3 板：分別處理前置韌體、extlinux／FIT 及載入媒體差異；共用命令產生器不代替這些專用流程。
4. Sunplus 2 板、Renesas 1 板、Realtek 2 板、Synaptics 1 板：完成原廠容器或特殊引導鏈適配，保留目前未知項，不用檔名猜格式。
5. 每家族的部署、啟動、短測及返回救援接線完成後，再用該家族首板核定資格；其餘板仍須逐板身份、媒體及周邊驗證。

本階段沒有任何新的完整平台後端或硬體資格宣告。原始映像、既有 444 筆工作與 45 個停用站點不變；F2P／F2S 的 20 筆版本未知也未被洗成成功。

## 續作 B：原配組件與生命週期

本次先推送計畫，再實作以下互不重疊的工作。保持原始映像及舊證據不變，不啟用站點、不重刷十套、不重建 444 套。

| 階段 | 工作及驗收 | 狀態 |
| --- | --- | --- |
| B0 | 本補充計畫先提交及推送 | 已完成，`30d711a8d` |
| B1 | 共用唯讀映像擷取：核對完整來源摘要、限制解壓大小、解析分割、擷取原配檔案；不掛載、不執行映像腳本 | 已實作，初階推送 `efab65617`；含證據重播共 37 項回歸通過，僅接受單一 MBR／ext 分割 |
| B2 | Allwinner ARM32／A64 的原配核心、initrd、DTB、環境及 overlay／fixup 處理；未支援條件明確阻擋 | 已實作限定路徑，55 項回歸通過；真 M64 準備通過，M1 保留區適配尚缺並維持阻擋 |
| B3 | Amlogic CM4／M2 Pro／M2S／M5 的原配組件及啟動差異處理；不沿用 H618 SRAM 或固定磁區 | 已實作限定原配組件及離線引導配置核對；62 項回歸、真 M5 組件準備通過，完整部署／救援仍待接入 |
| B4 | 將 0845 電源／UART 原型整理成有界生命週期工具，保存逐步證據及失敗；正常與故障返回分開，不以舊 SSH 身分代替本次配對 | 已實作獨立冷循環／引導／返回 runtime，58 項回歸及限定複審通過；首次登入、SSH 及舊階段整合仍未完成 |
| B5 | 整合 CLI、真實本機檔案擷取、離線回歸及限定安全審查，更新缺口並推送 | CLI 12 項回歸；整合 901 項及 Ruff 通過；三套真映像結果為二套準備通過、一套阻擋，隨本階段提交 |

各家族工具先接受唯讀檔案存取介面與明確板型，不能把同家族當成相同 DTB。引導位址、RAM 保留區及媒體配對仍由核定設定提供，不猜預設值。來源中的自訂腳本、未實作的參數、容器或檔案系統不得靜默省略。B2／B3 完成只表示有實際組件處理路徑，不自動等於完整部署與救援後端。

B4 的硬體入口保留 0845 限定與現有授權；程式新增不構成其他板的斷電或寫入授權。續作失敗須留下可追溯結果，不能直接接著重刷。Rockchip、MediaTek、SpacemiT 及其他家族仍依前述清單續作，不因完成這一批而宣稱全平台完成。

### B 階段交付及接續位置

共同入口見[原映像準備與重播](bananapi-lab-prepare-20260917.md)，家族模組見 [Allwinner](bananapi-lab-allwinner-20260917.md)、[Amlogic](bananapi-lab-amlogic-20260917.md)，0845 獨立 runtime 見[生命週期說明](bananapi-lab-h618-lifecycle-20260917.md)。[機器可讀交付紀錄](evidence/bpi-multiboard-components-20260917/completion.json)保存測試數、工具摘要及最新真映像結果。

本次 901 項為跨板工具 539 項加既有 H618 362 項。新增 224 項已包含在其中，不再相加。45 板／98 份既有來源守門仍通過，原 `platforms.json` 保持來源候選表定位，不把新組件模組誤填成完整後端。原硬體佇列、停用站點、十套歷史結果及客戶 XZ 均未改動。

| 真實來源 | 本次最新結果 | 證據目錄尾名 |
| --- | --- | --- |
| M1，Bookworm minimal，`6.18.49-current-sunxi` | `blocked`；發現標準動態 CMA，但 Allwinner 路徑尚未接入 | `m1-003-replay` |
| M64，Bookworm minimal，`6.18.46-current-sunxi64` | `prepared`；固定組件及超級區塊 UUID 核對通過 | `m64-003-replay` |
| M5，Bookworm minimal，`6.18.49-current-meson64` | `prepared`；動態 CMA 與固定韌體保留區分開記錄 | `m5-004-replay` |

以上目錄均在 `output/evidence/bpi-multiboard-components-20260917-*`。修正後採固定證據重播，不重新解壓原 XZ；所有結果明列沒有再次讀取來源。M1 早期 `prepared` 結果被後續較完整的保留區核對取代，不能選舊結果宣稱通過。

接續時先處理 M1 的 `/reserved-memory/default-pool`：大小 96 MiB、分配範圍起點 `0x40000000`、長度 `0x10000000`，未明示 `alignment`。須適配其動態 CMA 語意與核心配置，不能刪除節點、把整個分配範圍當成固定占用，或套用 Amlogic 的實板 RAM 位址。同時接 H618 的首次登入與同次 UART／SSH 身分換接，再完成排程契約；獨立 runtime 在父佇列已持鎖時不能直接重入。

之後依原清單擴展 Rockchip／MediaTek／SpacemiT 的組件與救援，並補其餘家族。這些仍有軟體工作，不是只差使用者提供板子。本次沒有新增實板資格、完整客戶原開機鏈通過或 444 套映像測試完成的宣告。

## 續作 C：完成可離線開發的整合

使用者要求持續實作，軟體完成後再集中提供實板驗證。本階段不以缺板為理由停止可離線工作，也不把拒絕未知條件的檢查器冒稱完整平台後端。計畫先推送，獨立模組分工實作，主代理整合並執行最終回歸。

| 階段 | 工作 | 目前狀態 |
| --- | --- | --- |
| C1 | Allwinner 標準動態 CMA、核心配置依據、原配組件重播與記憶體配置核對 | 已完成限定軟體實作及三項審查修正；79 項 Allwinner、18 項 CMA、35 項 U-Boot 通過，真 M1 與 M4 Zero EMAC 修正版重播均通過 |
| C2 | H618 首次登入、同次 UART 與 SSH 身分綁定、完整階段接線及中斷處理 | 已推送 `ec255671a`；H618、佇列、站點及來源守門共 287 項整合回歸通過，未新增硬體資格 |
| C3 | Rockchip／MediaTek／SpacemiT 原配組件及 extlinux 差異；保留原廠前置韌體邊界 | 組件及 Forge1／CM6 FIT 修正已推送 `a84b1d5fe`，91 項回歸通過；原入口完成真 ARM32／RISC-V U-Boot 編譯，K3 專用執行器續作中 |
| C4 | Sunplus／Renesas／Realtek／Synaptics 實際來源適配；未知格式須定位依據，不能只靠名稱猜測 | 六板組件、執行器及 Realtek M4／W2 真 BSP 編譯已推送 `8ac3cd640`；LMB 整合修正 `96ea489df`，103 項回歸通過；Realtek 獨立 SD 救援合併建置續作中 |
| C5 | 共用準備入口及跨板部署／引導／短測／救援整合，限定已明示且可驗證的站點契約 | 五階段、首次核定、中斷隔離、H618 獨立配對、原入口 FIT 及 Realtek SD 救援已實作；八組 212 項整合回歸通過；K3 後續獨立接入 |
| C6 | 逐項本機測試、真實來源離線核對、獨立審查、續作紀錄及分階段推送 | 進行中；磁碟 20 項及原映像 37 項回歸、Ruff、五項修正的限定複審通過 |

軟體驗收需列出可執行入口、具體支援格式及未覆蓋條件；不能只新增候選清單。硬體資格、媒體授權、首次救援部署、實體故障斷電及長期穩定性仍獨立列為待實板驗證。禁止為讓離線測試通過而刪除 CMA、忽略腳本語意、猜測 RAM／MMC 位址，或放寬原映像摘要與媒體身分核對。原始映像、既有證據及 45 個停用站點保留不動。

### C 階段即時紀錄

計畫先推送 `56c81130c`。首項交付為 GPT／MBR 多分割讀取器與 FAT／ext 開機區路由，不是完整家族後端。M7 真映像已確認 GPT 及原配核心／initrd／DTB，家族模組序列化失敗保留在 `output/evidence/bpi-multiboard-integrate-20260917-m7-001`，不得列為準備通過。W2 真映像已完成雙分割唯讀擷取，保留在 `output/evidence/bpi-multiboard-integrate-20260917-w2-extraction-001`；完整來源與原始映像摘要已核對，沒有執行原廠引導鏈。

磁碟模組 20 項、既有共用擷取 37 項及 Ruff 通過，命令輸出位於 `output/evidence/bpi-multiboard-integrate-20260917/`。獨立審查找到並修正暫存檔重開身分、跨分割連結假缺檔、非正規 fstab 路徑、混合 MBR 及來源增長上限五項問題；限定複審重現全部拒絕。此批未操作任何硬體，其他 C 階段繼續實作。

讀取器已推送 `3a12ea361`，根標籤證據已推送 `a7ba7439c`。C1 綁定端重新核對核心與內嵌 config，動態 CMA 缺少內嵌配置證據時拒絕；H618 來源不再把註解或未知賦值當作有效設定。主代理的持久重播結果為 `bpi-multiboard-integrate-20260917-m1-002-replay` 與 `bpi-multiboard-integrate-20260917-m4z-emac-002-replay`，兩者皆 `prepared`、`source_reread=false`，不是實板測試。

真實 M7、R3、W2、AI2N、M6 與 F2P 的完整映像來源另完成離線組件核對。F2P 從核心與 initramfs 解析出 `5.4.35-legacy-sunplus-sp7021-bpi`，不是信任檔名的 `0`；原 20 筆佇列阻擋未修改。首次單板授權入口、跨架構原生救援建置、原入口與特殊平台執行器仍在整合及獨立審查，不能提前宣告全平台完成。

2026-09-18 續作補上根標籤覆蓋範圍：含未辨識分割時不得宣告整張映像標籤唯一，舊證據缺少完整性欄位也不能放行。磁碟 21 項及共用擷取 37 項回歸通過。W2 因需要新增分割完整性證據，完成一次新的唯讀擷取 `bpi-multiboard-integrate-20260917-w2-003`；結果 `prepared`，原始壓縮與解壓內容摘要不變。舊 `w2-002` 不覆寫、不用來宣稱新的標籤完整性已核對。

跨架構原生救援建置已加入 ARM32、ARM64、RISC-V 的 ELF 與模組／韌體配置核對。審查發現的 FIFO 讀取阻塞及前檢後韌體替換均已補拒絕；新增 13 項及舊救援 17 項回歸通過，尚未執行新的實板原生建置。原入口及特殊家族執行器、H618 救援／客戶 DT 分流、排程故障返回與首次資格審查繼續整合。

根標籤補強已推送 `f9643e1fa`；跨架構救援及韌體複製補強已推送 `12d1f524d`，兩批限定複審無剩餘阻擋。中途整合回歸執行 929 項，其中一項本機真組件測試需另明示開關；來源守門如預期拒絕尚未重審更新的 H618 工具摘要，不能把這次記為全套通過。其餘板型唯讀抽樣正進行，所有原始映像及先前失敗證據保留。

每板一份的真實組件抽樣已涵蓋 45 板，44 份 `prepared`、一份 `blocked`；包含既有成功擷取重播及新增完整映像擷取，不代表本輪重新讀取全部壓縮檔，也不代表 444 套或任何新增實板測試通過。[逐板抽樣清單](evidence/bpi-multiboard-integrate-20260917/sample-audit.json) 固定來源、準備與擷取摘要，將目前結果與舊失敗紀錄分開。

R2 的 Bookworm minimal `6.6.153-current-mt7623` 原始環境指定 `/boot/dtb/mediatek/mt7623n-bananapi-bpi-r2`，缺少 `.dtb`；重新完整擷取確認此路徑不存在，證據為 `allboard-bpi-r2-004`。這是原映像缺檔問題，不再寫成解析器未實作；不得偷偷改選 `.dtb` 後宣稱原入口通過。M64 新必要 config 已重新擷取核對；M1 Plus 的 6.18.46 CMA 語意由相同上游來源確認；M4 Zero 的無線 overlay 型號變更已重算原始 DTB／DTBO，85 項回歸及限定複審通過，推送 `c52dfbc71`。

共用後端、首次核定與原廠 ABI 尚在收斂：發布與租約釋放間的硬停窗口、原始採樣重驗、LMB 實收範圍、CM6 FIT 與 K3 原入口必須完成各自回歸。抽樣 `prepared` 不解除這些軟體缺口，也不啟用停用站點。

後續收斂已修正發布／租約硬停窗口、原始採樣重驗、LMB 實收範圍及 CM6 FIT 投影；
原入口相關 85 項與共用後端八組 212 項均通過，兩組互有重疊，不相加。
原入口 ARM32／RISC-V 真編譯核對已推送 `2efc5bdd5`。
Realtek M4／W2 已完成客戶與 SD RAM 救援命令合併後的真 BSP 編譯及大小檢查；
主代理重新核對生成來源及產物，合成配對產物仍禁止部署。
K3 專用原廠入口及 D 階段外部媒體繼續實作，沒有將軟體未完成項目改稱只差實板。

## 續作 D：無 eMMC 板的外部測試媒體

目的仍是測試既有客戶映像，不新增 SD 切換器、不要求每套插拔 SD。固定 SD 保存救援入口，USB／NVMe 整碟作為另經備份與授權的可覆寫測試區。這是補上跨板流程的軟體缺口，不是已取得這些外部媒體的授權。計畫先提交及推送，實作不接硬體；之後每片板與每顆媒體仍由實體配對確認。

| 階段 | 實作及驗收 | 狀態 |
| --- | --- | --- |
| D0 | 本計畫先推送 | 已完成，`8de896db6` |
| D1 | USB／NVMe 結構化身分、sysfs 與描述符核對、掛載／swap／holders 排除，拒絕錯碟與熱拔插漂移 | 實作中，媒體盤點及固定契約已建立 |
| D2 | 完整備份與摘要、唯讀前檢、明示範圍寫入及完整回讀；預設不覆寫、不自動還原或重試 | 實作中，與 D1 共用實體識別 |
| D3 | 外部根與固定 SD 的 Linux／UART／SSH 同次身分核對；必要測試用 initramfs 保護層另記衍生摘要 | 實作中，與 D1 的觀測模式分工整合 |
| D4 | 將載入來源、救援來源與根媒體分開；接入五階段、首次核定、資源隔離、故障返回及續作 | 實作中，保留既有 eMMC 後端的獨立契約 |
| D5 | 小型假 sysfs／I/O、合成映像與故障注入整合回歸，限定審查及操作文件，分批推送 | 待實作 |

媒體契約不得把 `/dev/sdX` 或 `/dev/nvmeXnY` 當作穩定身分，也不得產生虛構 CID 以套用 eMMC 驗證器。USB 核對磁碟 WWID 與 USB serial、VID/PID、LUN、固定拓撲；NVMe 核對 serial、model、NSID、namespace WWID 與 PCIe 拓撲。缺少可區分內部磁碟的識別時拒絕，不把橋接器 serial 單獨當作磁碟保證。

首版限定 512 位元組邏輯磁區、直接 ext 根與可完整解析的 MBR 原映像。GPT 尾端搬移、殘留 metadata 清理、LVM／加密／網路根等不得默認進入；若支援需另有明確寫入計畫與回歸。目標容量由 sysfs、描述符裝置號及 ioctl 交叉核對；寫入限定已核定範圍，不修改其他裝置或 SD 救援。

固定 SD 核對完整摘要，保護層在交接客戶根前核對原配根 UUID／父媒體、禁止 `/boot` 或 swap 指向 SD，設定並確認核心暫時唯讀。這不是硬體防寫；載入測試用保護層時必須標為衍生 initramfs，不能宣稱完全等同客戶原始開機鏈。原 XZ 不修改，原核心、DTB、根系統及衍生測試層摘要分開保存。

USB／NVMe 規格與驅動支援仍需兩套核心及實體埠驗證；serial／WWID 並非防偽認證。沒有可核定的載入、救援或 RAM 配置時保持阻擋。遠端 writer 未能證明停止時維持隔離，不能因外部媒體而放寬自動切電規則。以上離線功能完成不會讓 45 個停用站點自動啟用。
