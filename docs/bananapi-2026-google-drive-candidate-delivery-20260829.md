# Banana Pi 2026.08 Google Drive 候選交付與空間整理紀錄

日期：2026-08-29

## 結論

- 本機已建立可直接選取上傳 Google Drive 的單一目錄：
  `/media/pi/SMCI/bpi/google-drive-upload/2026/2026.08`。
- 目錄含 45 個英文板型子目錄、444 個 `.img.xz`、444 個同名
  `.img.xz.sha` 與 45 份繁體中文發行說明。
- 目錄不含未壓縮 `.img`、逐映像 `.img.txt`、巢狀子目錄或符號連結。
- 全部 444 個映像已在整併前後各完成一次 SHA-256 與 XZ 串流全量驗證；
  中央副本的 inode 連結數均為 1。
- 此成果是 L2 內部驗證候選，不代表 45 個板型均已通過實機、全介面、
  長時間壓力、量產或再散布審查。

## 完整矩陣

- 其中 42 個板型各有 10 個映像：Bookworm、Trixie、Jammy、Noble、Resolute，
  每個發行版各有命令列與 XFCE 桌面版。
- `bpi-cm6`、`bpi-f3`、`bpi-sm10` 為 RISC-V 64 位元板型，本建置樹不提供該架構的
  Debian 12 Bookworm，因此各有其餘四個發行版的 8 個映像。
- 總數為 `42 * 10 + 3 * 8 = 444`。
- 板型、發行版、分支、命令列／XFCE 變體與檔名命名規則均通過精確矩陣檢查。

## 本次新增建置

本次補齊 10 個板型的 94 個映像，三個批次都沒有失敗列：

| 批次 | 板型 | 結果 | 摘要 SHA-256 |
| --- | --- | ---: | --- |
| 主批次驗證 `20260829T043902Z` | `bpi-aim7`、`bpi-cm5pro`、`bpi-forge1`、`bpi-m1super` | 40/40 | `9282737d4b2b706ded2f110f08a4c36bb71e05924b4d6b286e7676e47be76e29` |
| RISC-V `20260828T193340Z` | `bpi-cm6`、`bpi-f3`、`bpi-sm10` | 24/24 | `4b72445947a8a473b6667ee0a200ff7d1eab8bd8dd742d21ac8946370ee7f010` |
| 其他平台 `20260829T034226Z` | `bpi-ai2n`、`bpi-f2p`、`bpi-p2pro` | 30/30 | `e83970652ecdf5ffceaa8ef4fd06afc1e7663fd633eee4acdca55373935f7a38` |

三份摘要副本與它們的雜湊清單位於：

`/media/pi/SMCI/bpi/google-drive-upload/manifests/build-batches-20260829`

主批次原始建置 `20260828T152621Z` 已完成 40/40，但長時間作業期間建置啟動腳本被修改，
在所有列成功後出現腳本結尾語法錯誤。上表改採當前提交的乾淨重跑結果作為主批次驗證證據，
並由集中器逐映像檢查原始成功建置日誌。

## 代碼修正與版本

- 最終建置分支：`bananapi-family-optimization-20260826`。
- 最終映像代碼提交：`49ca988b5002de5f227d3f4a895dfecf46aad7ef`。
- RISC-V 批次提交：`59931102b205821b088d276183fb50cfd814fa9c`。
- `1651c12be`：僅在 `brltty` 服務實際存在時才停用，避免 AI2N 根檔案系統建置中斷。
- `49ca988b5`：AI2N 的 `bl2_bp_sd.bin` 與 `fip.bin` 改從映像內已安裝的 U-Boot 軟體包取得，
  不再依賴短暫存在的 U-Boot 工作樹。

## 完整性與目錄守門

已執行的確定性檢查：

1. `stage-completed-release-batches.sh`：核對 94 個新映像的批次摘要、成功建置日誌、矩陣鍵與 SHA-256。
2. `validate-bananapi-release.sh`：核對 45 個板型、444 個映像、發行版、分支、變體、檔名與發行說明。
3. `consolidate-release-hardlinks.sh`：完整驗證 444 份 SHA-256 與 XZ 串流，再依 inode 證據移除 554 個外部別名。
4. `finalize-bananapi-release.sh`：在工作樹與專用快取刪除後，再次完整驗證 444 份 SHA-256 與 XZ 串流。
5. `bash -n` 與 ShellCheck：集中、驗證、整併與完成腳本全部通過。

最終計數：

| 項目 | 數量 |
| --- | ---: |
| 板型目錄 | 45 |
| `.img.xz` | 444 |
| `.img.xz.sha` | 444 |
| 繁體中文發行說明 | 45 |
| 規則生效前已存在且原樣保留的英文說明 | 35 |
| 未壓縮 `.img` | 0 |
| 逐映像 `.img.txt` | 0 |
| 共用 inode 的中央映像 | 0 |

## 文件語言規則

最新專案規則要求本次新增或實質改寫的自然語言內容一律使用繁體中文。
因此：

- 45 個板型均有最新繁體中文說明。
- 原先 35 個板型已存在的英文說明原樣保留，本次未改寫。
- 新增 10 個板型沒有產生新英文自然語言內容。
- 若要對外補齊英文說明，必須先就該項交付給予明確的語言例外，再由受控流程產生與審查。

## 空間整理結果

- 移除 554 個可由中央副本完整解釋的外部硬連結別名。
- 移除兩個已完成、無未提交修改且摘要已封存的臨時 Git 工作樹。
- 移除一個僅供本批建置的主建置覆疊快取。
- 三個路徑的邏輯大小合計 `132,330,304,459` bytes。
- 可用空間實際增加 `133,655,949,312` bytes，約 125 GiB；使用率由 96% 降至 93%。
- 共用主快取、準備分支工作樹、硬體證據、來源樹與沒有新版完整替代品的舊矩陣均保留。

完整清理證據位於：

`/media/pi/SMCI/bpi/storage-cleanup-20260828`

## 對外發布 Gate

下列 17 個板型仍有未閉合的公開發布或再散布 Gate，不得交付客戶或公開散布：

`bpi-ai2n`、`bpi-aim7`、`bpi-cm5pro`、`bpi-cm6`、`bpi-f2p`、`bpi-f2s`、`bpi-forge1`、
`bpi-m1super`、`bpi-m4`、`bpi-m6`、`bpi-r1`、`bpi-r2`、`bpi-r3mini`、`bpi-r4lite`、
`bpi-r4pro`、`bpi-sm10`、`bpi-w2`。

下列 12 個板型有已知的條件限制，必須依各板型發行說明與對應硬體證據閉合：

`bpi-f3`、`bpi-m1p`、`bpi-m4b`、`bpi-m4z`、`bpi-m5pro`、`bpi-m7`、`bpi-p2pro`、
`bpi-r2pro`、`bpi-r3`、`bpi-r4`、`bpi-r64`、`bpi-w3`。

其餘 16 個板型也只是完成映像建置與檔案完整性守門，尚未因此達成 L3 實機驗證。

## 上傳邊界

手動上傳時只選取下列目錄：

`/media/pi/SMCI/bpi/google-drive-upload/2026/2026.08`

不要上傳相鄰的 `logs`、`manifests`、`templates` 或整理腳本；這些是內部可追溯與重跑資料。
在任一板型完成實機與授權 Gate 前，上傳位置應維持私密且標示為內部候選。
