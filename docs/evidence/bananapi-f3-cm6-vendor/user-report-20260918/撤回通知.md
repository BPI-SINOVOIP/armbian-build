# 2026-09-18：撤回舊 Titan eMMC 候選

使用者回報 CM6 在 SpacemiT Titan 的分區表匹配階段失敗：`Code 20014`、`partition table is null`。這是實際工具／硬體測試失敗；先前 108 項離線檢查沒有涵蓋 Titan 的真實匹配行為，不能用來證明可燒錄。

- **CM6 eMMC：已收到實際失敗回報，此候選錯誤／不可用。**
- **F3 eMMC：使用者回報套件不可用，且共用相關封裝設定，標記錯誤／不可用；目前缺少獨立 F3 完整燒錄日誌。**
- **兩份 SD：仍屬未完成實機驗證的候選；不能由本次 eMMC 結果推定其成敗。**

原 ZIP、SHA-256、manifest 與當時的離線驗證報告完整保留作追溯，禁止以原檔名覆寫。不要再將本批描述為可直接 Titan 燒錄的完成品。新的修正版本仍須另取得實際 Titan 匹配、USB 燒錄及開機證據。

證據角色：`titan-flash-error-code-20014.jpg` 是失敗畫面；`titan-selected-package.jpg` 是正確的操作／選檔參考，不是第二次失敗，也不是燒錄完成證據。Titan 版本與完整日誌尚待提供。

修正對象是本專案產生的燒錄套件；這次回報不歸因於使用者操作，正確參考畫面也不作為改按鈕或更換操作流程的依據。
