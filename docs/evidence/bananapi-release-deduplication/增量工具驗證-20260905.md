# Banana Pi 增量防重工具驗證紀錄

日期：2026-09-05

## 驗證對象

- `tools/audit-bananapi-release-state.py`
- `tools/prepare-bananapi-incremental-state.py`
- `tools/run-bananapi-incremental-queue.sh`
- `tools/rebuild-bananapi-latest-release.sh`

## 靜態檢查

執行下列檢查：

```bash
bash -n tools/rebuild-bananapi-latest-release.sh tools/run-bananapi-incremental-queue.sh
shellcheck tools/rebuild-bananapi-latest-release.sh tools/run-bananapi-incremental-queue.sh
ruff check tools/prepare-bananapi-incremental-state.py tools/audit-bananapi-release-state.py \
  tests/test_bananapi_incremental_state.py tests/test_bananapi_incremental_queue.py \
  tests/test_bananapi_release_state_audit.py
git diff --check
```

結果：全部通過。

## 回歸測試

執行命令：

```bash
python3 -m unittest discover -s tests -p 'test_*.py'
```

結果：650 項測試全部通過，無失敗及錯誤。

## 實際盤點結果

- 受控矩陣：45 塊板、444 個唯一映像項目。
- 本輪完成：94 個。
- 本輪待建：350 個。
- 既有正式映像：444 個，只作待取代基線。
- 舊四路全矩陣建置已停止，未在本次驗證中啟動映像建置。

固定帳本位於 `scan-20260905-c867`；後續每塊板執行前後都必須重新產生執行期
帳本，並確認該板待辦歸零且全域待辦單調下降。
