# 全矩陣候選收尾紀錄

紀錄時間：2026-09-07 12:35，Asia/Taipei。

## 實際進度

45 塊板、444 個候選映像已完成建置及壓縮。候選目錄有 444 個 `.img.xz`、
444 個同名 SHA 邊車與 45 份繁體中文發布說明；狀態目錄有 444 份項目標記、
45 份板級標記，`raw-items` 與 `transactions` 皆為空。
正式發布替換及全局清理尚未完成，不能把候選檔案數量當成正式發布完成證據。

- 候選：`/media/pi/SMCI/bpi/bananapi-release-c8673931c96c`
- 狀態：`/media/pi/SMCI/bpi/bananapi-state-c8673931c96c`
- 正式目錄：`/media/pi/SMCI/bpi/google-drive-upload/2026/2026.08`
- 矩陣 SHA-256：`bc476648e1e6ca96c931b2539abafb18b28ffefc21eff74df02456da0f807b2b`

434 個映像來自 `c8673931c96c23c510dd29a28440e77f0b03286f`；M5 Pro 的
10 個映像來自 `4a8086c3cd1a4f92a80ccb1d896f527ca4f5f6e1`。
必須使用逐板輸入政策保留兩組建置身分，不能更改標記宣稱全部來自同一提交。

## 已處理的收尾問題

1. 增量佇列把中文 TSV 標頭誤認成未完成板卡，導致已完成 44 板後回報失敗。
   `b0fc95590` 已修正標頭判讀；不需要重建已完成映像。
2. M5 Pro 十個映像完成後，嚴格政策產生器拒絕 104 份缺少 `status` 的舊格式
   標記。`25eb05911ff504486cd2c6fc7d3cfa136cd1a829` 加入受控遷移工具，
   先保存原始標記及工具快照，完整驗證後才原子補齊完成狀態。
3. 正式發布驗證器原先用 `|` 組合唯一鍵，真實稽核器及受控計畫使用 `/`。
   已以真實 `ItemKey` 定義重現失敗，再修正發布驗證器；19 項發布收尾回歸通過。

## 驗證證據與限制

遷移修正在 758 項完整回歸、靜態檢查及獨立審查通過後推送。
真實候選結構稽核得到 444 列採用映像、45 列完整板卡及零待辦。
主要日誌的 444 份檔案與標記 SHA-256 全部相符。

104 份舊標記中，10 份具有框架日誌雜湊，另外 94 份未留存受控框架副本或其
雜湊。這 94 份沿用既有計畫接受的完整主要日誌證據；不得補造框架日誌或
`completed_utc`。遷移另記實際遷移時間，不把它當成建置完成時間。

2026-09-07 11:48 啟動 `bananapi-item-status-migration-20260907.service`，
以已落盤快照執行完整 SHA-256 及 XZ 串流稽核。12:31 仍在執行，尚未修改舊標記。
該稽核依矩陣逐板交錯檢查既有正式及新候選檔案。當時磁碟剩餘約 416 GiB。

這些是建置、檔案內容與發布流程證據，不代表 45 塊實物全部通過硬體功能驗證。

## 接續順序

1. 確認遷移服務成功，以及正式遷移證據中的完整性稽核、104 份原始備份和零待辦。
2. 依既有完成標記接續 M5 Pro 收尾，產生完整逐板政策；已存在的十個映像沿用。
3. 將已測試的收尾提交整合並推送至 `bpi-integration-20260829`。
4. 執行 `tools/finalize-bananapi-mixed-source-release.sh`，完成候選稽核、正式
   目錄交易替換及正式矩陣驗證。
5. 正式結果確認後依盤點清冊封存 R1、清理舊產物，再推送最終結果。

查詢目前服務：

```bash
systemctl status bananapi-item-status-migration-20260907.service
journalctl -u bananapi-item-status-migration-20260907.service --no-pager
```

預定遷移證據目錄：
`/media/pi/SMCI/bpi/bananapi-state-c8673931c96c/migrations/item-status-20260907`。
只有該目錄的執行狀態與稽核結果實際成功，才能進入下一步。

## 自動接續設定

2026-09-07 12:51，並行稽核與發布接線的整體回歸為 766 項全部通過，耗時
333 秒；Ruff、ShellCheck 與差異檢查通過。已確認 2／4 工輸出一致，以及
映像 SHA、XZ、日誌 SHA 及工作例外皆會阻止成功。未據此宣稱實際加速倍率。

一次性執行設定保存在本文件旁的 `runtime-20260907/`。部署時只安裝到
`/run/systemd/system`，不設定開機自動啟動：

1. `migration-on-success.conf`：遷移成功後啟動既有 M5 Pro 延後階段。
2. `m5pro-on-success.conf`：M5 Pro 收尾成功後啟動正式發布；改為成功退出後
   進入 inactive 狀態，讓 `OnSuccess` 能觸發。
3. `bananapi-final-release-20260907.service`：啟動前再次讀取遷移成功狀態，
   再以 `VERIFICATION_WORKERS=4` 執行已驗證的正式收尾工具。

只有前一服務成功才由 `OnSuccess` 自動接續。任一失敗都須檢查日誌及保留現場；
服務成功退出仍須核對工具產生的正式稽核，才算本輪完成。

```bash
systemctl show bananapi-item-status-migration-20260907.service -p OnSuccess
systemctl show bananapi-m5pro-deferred-4a8086c3.service -p OnSuccess -p RemainAfterExit
systemctl status bananapi-final-release-20260907.service
journalctl -u bananapi-final-release-20260907.service --no-pager
```

發布結果完成後再處理舊資料及最終紀錄；成功銜接設定不代表這些收尾工作已完成。

2026-09-07 12:52 已完成一次性部署並以 `systemctl show` 讀回：遷移的
`OnSuccess` 指向 M5 Pro；M5 Pro 的 `OnSuccess` 指向正式收尾且
`RemainAfterExit=no`；正式收尾處於已載入、尚未執行狀態，工作數為 4。
部署後遷移主程序仍為 `2665074`，並未重啟。整合分支當時已同步至
`2709fdcaadadfde721cb90927df0f63ede148911`。

## 核心版號標籤限制

F2P／F2S 共 20 個映像檔名含 `_legacy_0_`。這是封裝版號標籤，不能寫成
實際 Linux 版本。F2P 的 Trixie 精簡版受控日誌
`logs/bpi-f2p-trixie-minimal-9f7557ab-9541-411a-8a18-89fc33fb54cf.log`
記錄 uImage 版本為 `5.4.35`，initramfs 目標為
`5.4.35-legacy-sunplus-sp7021-bpi`；此處未把單一日誌延伸當成所有板卡的實機證明。
通用繁中說明產生器的表頭改用「檔名核心標籤」，並提供 `uname -r` 核對方式。
本批既有簡版繁中說明並沒有核心版號表格；依下列最新要求保持原件，不套用此
通用產生器覆寫。

## 中英文說明的特定補充要求

2026-09-07 13:04，使用者要求每塊板補一份英文 `Release-Notes`。本批固定為
45 份 `Release-Notes-zh-TW.md` 加上 45 份 `Release-Notes-English.md`；英文
例外僅用於本批逐板翻譯及必要的產生範本，其他計畫、紀錄、回覆與提交維持繁中。

已唯讀比對 45 份中文，全部符合相同的完整簡版格式，只在板名、來源提交、
核心分支、發行版與映像數等欄位不同。翻譯器逐字匹配已知中文正文，並核對矩陣
與目錄身分；遇到新段落或限制變動會拒絕整批寫入，不以寬鬆擷取漏掉內容。
英文保留同樣的軟體候選、未執行實機、完整介面、長壓、量產及再散布限制。

正式收斂流程改為：

1. 在既有發布鎖下記錄 45 份繁中原件雜湊。
2. 只建立或原子更新英文檔，核對繁中原件未變及英文全文翻譯一致。
3. 提升器精確要求兩份非空實體說明，透過硬連結連同映像提升。
4. 正式完整稽核後再核對繁中原件雜湊及英文全文；失敗仍回復舊正式目錄。

13:20 時尚未向真實候選寫入英文檔，因受控遷移仍持有候選鎖。沒有重啟遷移、
改寫既有稽核快照或重建任何映像。新工具將在遷移成功後由既有自動接續流程執行。

13:27 完成本輪回歸與獨立審查：

- `python3 -B -m unittest discover -s tests -p 'test_*bananapi*.py'`：
  686 項完成，預設略過一項需指定真實來源的唯讀檢查，無失敗，耗時 366.639 秒。
- 其餘不符合上述檔名規則的測試，以 `unittest` 逐檔載入共 110 項，全部通過。
- 設定 `BANANAPI_TRANSLATION_SOURCE` 為本輪真實候選根後執行翻譯測試：
  21 項全部通過，包含前述略過項，未寫真實候選或讀取映像本體。
- 發布整合測試新增正式中英文遭替換的兩個破壞案例後，8 項全部通過；同時
  驗證硬連結、45 板／444 映像與失敗回滾。這兩項新增案例另行執行，不回頭
  重跑已完成的大組回歸。
- Ruff、ShellCheck、`bash -n` 與 `git diff --check` 通過；獨立唯讀審查
  未發現實質問題，審查者另跑 9 項聚焦測試通過。

以上合計覆蓋目前 798 個唯一測試案例；軟體測試不等同實際發布完成或硬體驗證。

## 發布後清理盤點

2026-09-07 13:09 的獨立唯讀盤點估計，正式發布與重新稽核成功後，保留 R1
資料可望回收約 291.416 GiB。這不是已回收量，執行前須重新核對 inode、連結
數與檔案使用情形；開啟中的已刪除檔案可能延後空間釋放。

| 範圍 | 可望回收配置量 bytes | 限制 |
| --- | ---: | --- |
| `scan-20260905-c867/中止產物.tsv` 五筆 | 10,393,137,152 | 精確按既有清冊，含兩個 partial、兩個 IMG 與一個空標記 |
| 四個舊候選根其餘非共用檔 | 32,768 | 兩個 partial 不重算；188 個硬連結別名不回收映像本體 |
| 舊正式版本扣除 R1 | 302,511,980,544 | 僅限成功交易記錄中的精確 `previous`，不可清理新版正式路徑 |

四個舊候選根為 `/media/pi/SMCI/bpi/` 下的 `main-release-c8673931c96c`、
`parallel-release-c8673931c96c`、`parallel2-release-c8673931c96c` 與
`parallel3-release-c8673931c96c`。它們有 94 組映像與 SHA 邊車共 188 個檔案
與最新候選共用 inode，本體配置量約 59.274 GiB，不能重複當成可回收容量。

R1 現存於舊正式目錄，22 個檔案約 6.344 GiB；契約指定的舊映像根已不存在，
十份原始建置日誌仍在，但目前 R1 目錄缺契約要求的十份 `.img.txt`。不得宣稱
完整封存驗證通過，也不得隨其他舊正式映像刪除。成功切換後先保全 R1 與來源
日誌，再依實際證據處理封存缺口。
