# X2 在三星 S337 的標準啟動 G1 證據

## 結論

三星 `S337` 使用與 1116 相同的 X2 792 MHz 標準映像完成一次完全斷電冷啟動。
SPL 正確從雙 Rank 探測退回單 Rank，最終識別 2,048 MiB、x32、1 Rank、
16 Rows、10 Columns；TF-A、U-Boot、核心、initramfs、rootfs 與使用者空間
全部通過。

systemd 狀態為 `running`，失敗服務為零。另完成 180 秒、1.4 GiB 記憶體與
4 個 CPU worker 的冒煙測試，結束碼為 `0`，沒有 OOM、page fault、panic、
Oops、EDAC 或資料毀損訊息。

本結果通過 G1 並計入 G2 `1/10`。短壓力結果不能取代完整 G3 或兩小時 G4。

## 樣本與固定輸入

| 項目 | 值 |
| --- | --- |
| 樣本代號 | `S337` |
| DDR | 三星 `K4F6E3S4HM-MGCJ`，頂部追溯碼 `SEC 337` |
| 照片 | `hardware/M4Z-Samsung-20260813/IMG_3687.jpg` |
| 完整映像 SHA-256 | `fb665992d6a5becfe2694cade5f2e1367f0eeb18582fdcda8e8d3d446042610b` |
| SD 卡 | `SR64G`，`59.5 GiB`，與 1116 測試相同 |
| eMMC | `/dev/mmcblk2`，`7.3 GiB` |
| 板身序號與 PCB 版本 | 待補拍 |

SD 卡在 1116 第一次開機時已完成 rootfs 擴充與測試帳號初始化；切換 S337
前沒有更換 bootloader、核心、裝置樹或 rootfs 基礎系統。開機產生的日誌與
執行期狀態不視為測試映像版本變更。

## DDR 與啟動鏈

```text
M4ZDDR1_PROFILE0 clk=792 type=8 wl=0 rc=1 rt=0 wt=0
M4ZDDR1_PROFILE1 dx_odt=07070707 dx_dri=0e0e0e0e ca_dri=00000d0d odt_en=aaaaeeee
M4ZDDR1_PROFILE2 tpr6=3a808080 tpr10=402f6663 tpr11=24242422 tpr12=110f1111
M4ZDDR1_BEGIN ranks=2 width=32 rows=13 cols=8
M4ZDDR1_STAGE stage=rc result=fail attempts=5
M4ZDDR1_BEGIN ranks=1 width=32 rows=13 cols=8
M4ZDDR1_STAGE stage=rc result=pass attempts=1
M4ZDDR1_FINAL size_mib=2048 ranks=1 width=32 rows=16 cols=10
```

第一次雙 Rank read calibration 失敗五次是容量探測的一部分；程式隨即改用
單 Rank，後續四個 geometry 階段都一次通過。最終容量與照片料號的 16 Gb
單 Rank 預期一致，這不是 DDR 初始化失敗。

U-Boot 完成 initrd checksum、DTB、overlay 與核心載入。Linux
`6.18.32-current-sunxi64` 回報 `1914268K/2097152K` 可用記憶體，rootfs 為
`/dev/mmcblk0p1` ext4 讀寫掛載。

## 短壓力測試

```bash
stress --vm 1 --vm-bytes 1400M --vm-keep --cpu 4 --timeout 180s
```

```text
stress: info: successful run completed in 180s
STRESS_RC=0
```

測試後可用記憶體約 1.8 GiB，swap 使用量為零；核心日誌未命中 OOM、
page fault、panic、Oops、資料毀損、Data Abort 或 EDAC 關鍵字。這是冒煙測試，
尚未涵蓋全可用容量 pattern、溫度角落或記憶體與 MMC 長時間並行。

板上時鐘尚未透過網路校時，UART 中的系統日期為 2026-05-21；本證據以主機
建立目錄與擷取時間 2026-08-13 為準。

## 非 DDR 周邊異常

```text
mmc1: Failed to initialize a non-removable card
Bluetooth: hci1: command 0x0c03 tx timeout
Bluetooth: hci1: BCM: Reset failed (-110)
```

以上訊息未影響 SD、eMMC 枚舉、systemd 或記憶體測試，暫不判為 DDR Gate
失敗；但它們是 S337 的周邊支援缺口，後續 Wi-Fi／Bluetooth 功能驗證必須
另行處理，不能從本次 G1 宣稱無線功能正常。

## UART 證據

| 項目 | 值 |
| --- | --- |
| 本機原始檔 | `output/evidence/bpi-m4zero-opi-ddr/X2-hardware-S337-20260813-230457/uart-s337-x2-cold-01.log` |
| 原始大小 | `26008` 位元組、`488` 行 |
| 原始／解壓 SHA-256 | `f05639b286aa28ebda73d0ac8eb27fd77f125ce5848bd0056cd89aa7689753d3` |
| 提交版 | `hardware/X2-S337-20260813/uart-s337-x2-cold-01.log.gz` |
| 壓縮 SHA-256 | `8bb7539628a03c7dda6ff7ce5c3919b4596b8acb5e978474e5b08477b8f8188f` |

登入密碼輸入在 UART 上未回顯，因此提交的原始日誌不包含明文密碼，未做
內容改寫。

## 下一步

1. 以同一張 SD 卡驗證三星 `S322` 的首次完全斷電冷啟動。
2. 若 S322 通過 G1，再安排 0438、1116、S337、S322 交錯冷啟動矩陣。
3. 另建周邊議題追蹤 S337 的 SDIO 與 Bluetooth 初始化錯誤。
