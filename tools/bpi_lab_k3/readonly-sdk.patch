--- a/board/spacemit/k3/k3.c
+++ b/board/spacemit/k3/k3.c
@@ -177,0 +178,2 @@
+int read_mac_from_tlv(void);
+int get_chipid_from_efuse(uint64_t *chipid);
@@ -1077,2 +1079 @@
-	set_env_ethaddr();
-	set_dev_serial_no();
+	read_mac_from_tlv();
@@ -1084,10 +1085 @@
-	get_reboot_config();
-	run_fastboot_command();
-
-	if (BOOT_MODE_SD == get_boot_mode()) {
-		try_flash_image_from_card();
-	}
-
-#ifdef CONFIG_BOOT_FROM_USB_DISK
-	try_boot_from_udisk();
-#endif
+	/* 救援建置不清除 PMIC 持久旗標，也不自動燒錄或改選 USB。 */
@@ -1098 +1090 @@
-	import_env_from_bootfs();
+	/* 原 env_k3.txt 由受控命令在限長與 SHA-256 核對後匯入。 */
